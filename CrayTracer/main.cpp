

int main()
{
	//load scene, defining the camera, the geometry and associated materials (for now it's static scenes only no sequential rendering for video)
	//create a scene defnition in json/xml

	//partition scene using the accelerator (K/D tree? or something else?)

	//raytrace

	//write to image (probably using STB_image)

	//profit
	return 0;
}